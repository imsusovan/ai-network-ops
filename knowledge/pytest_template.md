# Pytest Template

Use yaml topology file.
Use docker exec through command_runner.py.
Each test should loop through all routers.
Each assertion should include node name and command output.
Never run destructive commands unless explicitly approved.
